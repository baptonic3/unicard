[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

[etherscan]
arbitrum_one = { key = "${ARBISCAN_API_KEY}" }

[rpc_endpoints]
arbitrum_one = "${NEXT_PUBLIC_ARB_RPC_URL}"
